# Core deps
fastapi
uvicorn[standard]
pydantic
httpx
playwright
dataclasses-json

# AI
ollama  # Optional Python client
faiss-cpu  # Vector store (future)
chromadb  # Vector store alt (future)
serper-api

# Utils
asyncio
pathlib

